import React, { useContext } from 'react'
import { ApiContext } from '../service/Api'

function PlantsList() {
  const {data, setData} = useContext(ApiContext)

  return (

    <div>

      
    </div>
  )
}

export default PlantsList
